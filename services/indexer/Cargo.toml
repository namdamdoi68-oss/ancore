[package]
name = "ancore-indexer"
version = "0.1.0"
edition = "2021"
rust-version = "1.74.0"

[lib]
name = "ancore_indexer"
path = "src/lib.rs"

[[bin]]
name = "ancore-indexer"
path = "src/main.rs"

[dependencies]
# Web framework
axum = "0.8"
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.7", features = ["trace", "cors"] }
tower_governor = "0.8"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Database
sqlx = { version = "0.9", features = ["runtime-tokio", "postgres", "chrono", "uuid", "json"] }
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1", features = ["v4", "serde"] }
base64 = "0.23"

# Error handling
thiserror = "2"
anyhow = "1"

# Async trait support
async-trait = "0.1"

# Tracing
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Metrics
metrics = "0.24"
metrics-exporter-prometheus = { version = "0.18", default-features = false, features = ["http-listener"] }

# Environment
dotenvy = "0.15"
stellar-xdr = { version = "27.0.0", features = ["curr"] }
reqwest = { version = "0.13.3", features = ["json"] }

[dev-dependencies]
sqlx = { version = "0.9", features = ["runtime-tokio", "postgres", "chrono", "uuid", "json", "macros"] }
tower = { version = "0.5", features = ["full"] }
http-body-util = "0.1"
hyper = { version = "1.0", features = ["full"] }
wiremock = "0.6.5"
